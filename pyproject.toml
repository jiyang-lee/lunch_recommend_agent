[project]
name = "lunch-recommend-agent"
version = "0.1.0"
description = "Local STT -> LLM -> TTS lunch recommendation agent"
requires-python = ">=3.11"
dependencies = [
  "openai",
  "python-dotenv",
  "gtts",
  "sounddevice",
  "scipy",
]
